export enum EnumSortParams {
	Newest = 'Newest',
	Alphabet = 'Alphabet'
}

export enum EnumThunkStatus {
	Pending = 'pending',
	Fulfilled = 'fulfilled',
	Rejected = 'rejected'
}

export enum EnumMessageType {
	Text = 'text',
	Link = 'link',
	Image = 'image'
}